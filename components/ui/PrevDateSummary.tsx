import React from "react";
import styled from "styled-components";

const PrevDateSummary = () => {
  return <Container>PrevDateSummary</Container>;
};

const Container = styled.div`
  display: grid;
`;

export default PrevDateSummary;
